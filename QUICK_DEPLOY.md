# ⚡ Быстрый деплой на REG.RU

## 🎯 За 10 минут

### 1️⃣ Закажите VPS на REG.RU
- Тариф: 2 CPU, 4 GB RAM, 40 GB SSD
- ОС: Ubuntu 22.04 LTS
- Получите IP адрес (например: `123.45.67.89`)

### 2️⃣ Подключитесь к серверу
```bash
ssh root@123.45.67.89
```

### 3️⃣ Установите Docker (одной командой)
```bash
curl -fsSL https://get.docker.com -o get-docker.sh && \
sudo sh get-docker.sh && \
sudo usermod -aG docker $USER
```

### 4️⃣ Загрузите проект

**Вариант A - Через Git (рекомендуется):**
```bash
# Создайте репозиторий на GitHub и загрузите туда проект
# Затем на сервере:
git clone https://github.com/YOUR_USERNAME/lockgo.git
cd lockgo
```

**Вариант B - Через SCP (быстро):**
```bash
# На локальном компьютере:
cd /Users/danila/Desktop/Lock\&Go/Firmware+back+front
tar -czf lockgo.tar.gz lock-go/
scp lockgo.tar.gz root@123.45.67.89:~

# На сервере:
tar -xzf lockgo.tar.gz
cd lock-go
```

### 5️⃣ Запустите приложение
```bash
# Создайте необходимые директории
mkdir -p data mosquitto/{config,data,log}

# Запустите
docker compose up -d

# Проверьте
docker compose ps
```

### 6️⃣ Настройте домен
В панели REG.RU добавьте A-записи:
```
Тип    Имя    Значение           TTL
A      @      123.45.67.89       3600
A      www    123.45.67.89       3600
```

### 7️⃣ Готово! 🎉
Откройте в браузере:
- `http://ваш-домен.ru` - H5 приложение
- `http://ваш-домен.ru?admin=true` - Админка

---

## 🔒 Добавить SSL (опционально)

```bash
# Установите Certbot
sudo apt install -y certbot

# Остановите контейнеры
docker compose down

# Получите сертификат
sudo certbot certonly --standalone -d ваш-домен.ru -d www.ваш-домен.ru

# Скопируйте сертификаты
sudo mkdir -p nginx/ssl
sudo cp /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem nginx/ssl/
sudo cp /etc/letsencrypt/live/ваш-домен.ru/privkey.pem nginx/ssl/

# Запустите с SSL
docker compose up -d
```

Теперь доступно по `https://ваш-домен.ru` 🔐

---

## 📱 Как тестировать

1. **Откройте H5:** `https://ваш-домен.ru`
2. **Откройте DevTools:** `Cmd/Ctrl + Shift + I`
3. **Включите мобильный режим:** `Cmd/Ctrl + Shift + M`
4. **Выберите устройство:** iPhone 14 Pro Max
5. **Тестируйте!** 🚀

---

## 🔄 Обновление приложения

```bash
# Зайдите на сервер
ssh root@123.45.67.89
cd lockgo

# Получите изменения (если через Git)
git pull

# Или загрузите новую версию через SCP
# scp lockgo.tar.gz root@123.45.67.89:~

# Перезапустите
docker compose down
docker compose up -d --build
```

---

## 📊 Полезные команды

```bash
# Логи
docker compose logs -f

# Статус
docker compose ps

# Перезапуск
docker compose restart

# Остановка
docker compose down

# Полная очистка и перезапуск
docker compose down -v
docker compose up -d --build
```

---

## 🆘 Проблемы?

### Не открывается сайт
```bash
# Проверьте статус
docker compose ps

# Проверьте логи
docker compose logs backend
docker compose logs frontend

# Проверьте порты
sudo netstat -tulpn | grep -E '80|443|8000'
```

### Ошибка 500 в админке
```bash
# Перезапустите backend
docker compose restart backend

# Проверьте логи
docker compose logs backend --tail 50
```

---

## 📞 Контакты REG.RU

- **Телефон:** 8 (495) 580-11-11
- **Email:** support@reg.ru
- **Сайт:** https://www.reg.ru

---

## ✅ Итог

После деплоя у вас будет:
- ✅ Рабочее H5 приложение
- ✅ Админ панель
- ✅ WebSocket для real-time обновлений
- ✅ MQTT для железа
- ✅ SSL сертификат (опционально)

**Полная документация:** см. `DEPLOYMENT_REGRU.md`

🚀 **Успешного деплоя!**

